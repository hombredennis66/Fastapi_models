## 2025-05-22 - [LLM 推論のキャッシュによる高速化]
**Learning:** Hugging Face の `pipeline` による推論は計算コストが高いため、同一の入力に対しては結果をキャッシュすることで劇的なパフォーマンス向上が見込める。`functools.lru_cache` をインスタンスメソッドに適用する場合、そのインスタンスがシングルトン的に扱われるのであればメモリリークのリスクは低い。
**Action:** 再帰的な呼び出しや高頻度の同一入力が想定される推論処理には、まずキャッシュの適用を検討する。
